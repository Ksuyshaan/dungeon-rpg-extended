# dungeon-rpg-extended

---
```markdown
# 🏰 Практическая работа №7: Мини-RPG — жизнь в подземелье

> *Расширенная RPG с **предметами**, **исключениями**, **коллекциями** и паттернами **Decorator**, **Observer**.*

---

## 🎯 Цель
Освоить работу с:
- собственными исключениями,
- коллекциями (`List`, `Map`, `Iterator`),
- паттернами Decorator (улучшение зелий) и Observer (UI-лог),
- композицией vs наследованием.

---

## ✅ Реализовано
- **Инвентарь** на 5 предметов (`List<Item>`).
- Предметы через интерфейс `Item`:
  - `Potion` → `GreaterPotion` (паттерн **Decorator**).
- Собственные исключения:
  - `LowHealthException` (hp ≤ 20),
  - `InventoryFullException`.
- Статистика побед в `Map<String, Integer>`.
- Пошаговый бой с выбором: атака / предмет / побег.

---

## 🖥️ Пример работы
```text
[UI] Легонакс подобрал: Улучшенное Зелье.
Ваш инвентарь:
1. Улучшенное Зелье
Выберите предмет: 1
[UI] Легонакс получил 40 урона! HP: 100

---

## Запуск
mkdir -p bin
javac -d bin src/**/*.java
java -cp bin DungeonGame
